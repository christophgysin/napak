var $ff35ac283ca150a59ff919b331c58a71$exports = {};
const $ff35ac283ca150a59ff919b331c58a71$export$store = {
  supported: (() => {
    return 'localStorage' in window;
  })(),
  format: function format() {
    localStorage.clear();
  },
  write: function write(params) {
    localStorage.setObject(params.key, params.keydata);
  },
  read: function read(params) {
    if (localStorage.getObject(params.key)) {
      return localStorage.getObject(params.key);
    } else return false;
  }
};

Storage.prototype.setObject = function (key, value, usedb) {
  try {
    this.removeItem(key);
    this.setItem(key, JSON.stringify(value));
  } catch (e) {
    console.log(e);
  }
};

Storage.prototype.getObject = function (key, usedb) {
  return JSON.parse(this.getItem(key));
};

$ff35ac283ca150a59ff919b331c58a71$exports.store = $ff35ac283ca150a59ff919b331c58a71$export$store;